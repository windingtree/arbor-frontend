import React from "react";

import { Container, Typography } from '@material-ui/core';

export default function WizardCreating(props) {
  return (
    <Container>
      <Typography variant={'h2'}>WizardCreating</Typography>
    </Container>
  );
}
