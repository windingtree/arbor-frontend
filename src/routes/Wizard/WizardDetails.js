import React from "react";

import { Container, Typography } from '@material-ui/core';

export default function WizardDetails(props) {
  return (
    <Container>
      <Typography variant={'h2'}>WizardDetails</Typography>
    </Container>
  );
}
