import React from "react";

import { Container, Typography } from '@material-ui/core';

export default function WizardHosting(props) {
  return (
    <Container>
      <Typography variant={'h2'}>WizardHosting</Typography>
    </Container>
  );
}
