import React from "react";

import { Container, Typography } from '@material-ui/core';

export default function WizardMetamask(props) {
  return (
    <Container>
      <Typography variant={'h2'}>WizardMetamask</Typography>
    </Container>
  );
}
