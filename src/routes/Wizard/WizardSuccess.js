import React from "react";

import { Container, Typography } from '@material-ui/core';

export default function WizardSuccess(props) {
  return (
    <Container>
      <Typography variant={'h2'}>WizardSuccess</Typography>
    </Container>
  );
}
